<ul class="projlist list fullwidth">
  <% for (const item of items) { %>
  <li <%="metadataAttrs(item)" %>
    >
    <table class="proj-table">
      <tr class="proj-title-row">
        <th colspan="4" class="proj-cell proj-meta">
          <h3><a href="<%= item.path %>"><%= item.title %></a></h3>
          <% if (item.links) { %>
          <div class="buttonrow">
            <% Object.keys(item.links).forEach(function (key) { %>
            <a class="meta-pill" href="<%= item.links[key] %>"><%= key %></a>
            <% }) %>
          </div>
          <% } %>
        </th>
      </tr>
      <tr class="proj-body-row">
        <td class="proj-cell proj-meta">
          <% if (item.image) { %>
          <img src="<%- item.image %>" />
          <% } else { %>
          <img src="../../images/logo.png" />
          <% } %> <% if (item.doi) { %>
          <div
            data-badge-popover="right"
            data-badge-type="donut"
            data-doi="<%= item.doi %>"
            data-hide-no-mentions="true"
            class="altmetric-embed"
          ></div>
          <% } %>
        </td>

        <td class="proj-cell proj-abstract">
          <p class="abstract"><%= item.abstract %></p>
        </td>
      </tr>
    </table>
  </li>
  <% } %>
</ul>
